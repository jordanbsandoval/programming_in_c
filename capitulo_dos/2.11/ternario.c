#include<stdio.h>

/**
 *
 *
 */

int main(void)
{
    int a = 2;
    int b = 3;
    int result = 0;

    (a > b) ? (printf("el numero mayor es: %d\n", a)) : (printf("el numero mayor es: %d\n", b));
    return (0);
}
