#include<stdio.h>

/**
 *
 *
 *
 */

int main(void)
{
    int c, i = 0;
    int number[];

    while ((c = getchar) != EOF)
    {
        if(c >= 0 && c <= 9)
            number[i] = c;
        else if(c == '+' || c == '-' || c == '*' || c == '/')
            
    }
}
